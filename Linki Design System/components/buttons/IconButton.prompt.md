IconButton — a square, icon-only button for toolbars, table rows, and panel headers. Always pass `label` for accessibility.

```jsx
<IconButton icon={<Icon name="settings" />} label="Settings" />
<IconButton icon={<Icon name="trash-2" />} label="Delete" variant="secondary" />
```
